services:
  - type: web
    name: pricky-backend
    env: python
    plan: free
    buildCommand: ""
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: TOGETHER_API_KEY
        value: your_key_here